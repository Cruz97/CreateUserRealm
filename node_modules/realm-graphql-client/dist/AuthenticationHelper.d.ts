import { Credentials } from "./Credentials";
import { User } from "./User";
export interface AccessToken {
    token: string;
    expires: number;
}
export declare class AuthenticationHelper {
    static authenticate(credentials: Credentials, server: string): Promise<User>;
    static refreshAccessToken(user: User, realmPath: string): Promise<AccessToken>;
    static revoke(user: User): Promise<void>;
    private static fetch;
    private static postHeaders;
}
