import { Credentials } from "./Credentials";
export declare class User {
    static authenticate(credentials: Credentials, server: string): Promise<User>;
    identity: string;
    isAdmin: boolean;
    server: string;
    token: string;
    constructor(init: Partial<User>);
    logOut(): Promise<void>;
}
